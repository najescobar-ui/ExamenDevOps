# Base URL de la API. Vacío = mismo origen (producción tras el ALB).
# En dev se deja vacío y Vite hace proxy de /api/v1/* a los backends locales.
VITE_API_URL=

# Solo para dev: destinos del proxy de Vite si los backends no están en localhost.
# VITE_VENTAS_TARGET=http://localhost:8080
# VITE_DESPACHOS_TARGET=http://localhost:8081
